import 'package:flutter/material.dart';

void main() {
  runApp(App());
}

class App extends StatelessWidget {
  Widget build(BuildContext ctx) {
    return MaterialApp(
      home: Text('First Flutter App'),
    );
  }
}
